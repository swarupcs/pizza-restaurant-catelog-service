# Catalog Service - Test Configuration
# This file is loaded automatically by the 'config' npm package when NODE_ENV=test

server:
    port: 5502

database:
    url: "mongodb://localhost:27017/catalog-service-test"

kafka:
    broker:
        - "localhost:9092"

frontend:
    clientUI: "http://localhost:5173"
    adminUI: "http://localhost:5174"

auth:
    jwksUri: "http://localhost:5501/.well-known/jwks.json"

s3:
    region: "ap-south-1"
    bucket: "catalog-service-test"
    accessKeyId: "test-access-key"
    secretAccessKey: "test-secret-key"
